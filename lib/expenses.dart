import 'package:flutter/material.dart';

class ExpensesMainScreen extends StatelessWidget {
  const ExpensesMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Ni háo ninja'));
  }
}
